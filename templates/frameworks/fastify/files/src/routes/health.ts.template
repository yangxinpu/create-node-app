import type { FastifyInstance } from 'fastify'

/* {{healthRouteHandlerSource}} */

export async function health(app: FastifyInstance): Promise<void> {
  app.get('/health', async () => getHealthStatus())
}
